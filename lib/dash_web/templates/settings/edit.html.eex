Edit Settings<h1>Edit User</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.settings_path(@conn, :update, @settings)) %>

<span><%= link "Back", to: Routes.user_path(@conn, :index) %></span>